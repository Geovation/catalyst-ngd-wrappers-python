[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[metadata]
name = "catalyst_ngd_wrappers"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "datetime==5.5",
    "requests==2.32.4",
    "shapely==2.1.1"
]
